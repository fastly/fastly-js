# user

User - JavaScript client for user
A user of the Fastly API and web interface. A user is always associated with a customer.
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 1.0.0
- Package version: 1.0.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install user --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your user from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var User = require('user');

var defaultClient = User.ApiClient.instance;
// Configure API key authorization: token_superuser
var token_superuser = defaultClient.authentications['token_superuser'];
token_superuser.apiKey = "YOUR API KEY"
// Uncomment the following line to set a prefix for the API key, e.g. "Token" (defaults to null)
//token_superuser.apiKeyPrefix['Fastly-Key'] = "Token"

var api = new User.AccountUserApi()
api.createUser().then(function(data) {
  console.log('API called successfully. Returned data: ' + data);
}, function(error) {
  console.error(error);
});


```

## Documentation for API Endpoints

All URIs are relative to *https://api.fastly.com*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*User.AccountUserApi* | [**createUser**](docs/AccountUserApi.md#createUser) | **POST** /user | Create a user
*User.AccountUserApi* | [**deleteUser**](docs/AccountUserApi.md#deleteUser) | **DELETE** /user/{user_id} | Delete a user
*User.AccountUserApi* | [**getCurrentUser**](docs/AccountUserApi.md#getCurrentUser) | **GET** /current_user | Get the current user
*User.AccountUserApi* | [**getUser**](docs/AccountUserApi.md#getUser) | **GET** /user/{user_id} | Get a user
*User.AccountUserApi* | [**requestPasswordReset**](docs/AccountUserApi.md#requestPasswordReset) | **POST** /user/{user_login}/password/request_reset | Request a password reset
*User.AccountUserApi* | [**updateUser**](docs/AccountUserApi.md#updateUser) | **PUT** /user/{user_id} | Update a user
*User.AccountUserApi* | [**updateUserPassword**](docs/AccountUserApi.md#updateUserPassword) | **POST** /current_user/password | Update the user&#39;s password


## Documentation for Models

 - [User.InlineResponse200](docs/InlineResponse200.md)
 - [User.ModelUser](docs/ModelUser.md)
 - [User.RelationshipUser](docs/RelationshipUser.md)
 - [User.RelationshipUserUser](docs/RelationshipUserUser.md)
 - [User.RelationshipUserUserData](docs/RelationshipUserUserData.md)
 - [User.RoleUser](docs/RoleUser.md)


## Documentation for Authorization



### session_or_token


- **Type**: API key
- **API key parameter name**: Fastly-Key
- **Location**: HTTP header



### session_password_change

- **Type**: HTTP basic authentication



### token


- **Type**: API key
- **API key parameter name**: Fastly-Key
- **Location**: HTTP header



### token_superuser


- **Type**: API key
- **API key parameter name**: Fastly-Key
- **Location**: HTTP header



### username_and_password

- **Type**: HTTP basic authentication

